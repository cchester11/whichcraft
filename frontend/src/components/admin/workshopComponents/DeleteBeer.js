import React from "react";

// This component needs:
// 1. A form
// 2. A submit button
// Functionality:
// 1. Either the admin can type in a beer name to the form input or a dropdown list can populate and the admin can select what beer to delete from the dropdown
// 2. A submit handler that will send a request to the delete beer api endpoint 

export default function DeleteBeer () {
      return (
            <div>Delete Beer</div>
      );
};